diff --git a/node_modules/react-native-draggable-masonry/.bun-tag-9334ed9625a21b7f b/.bun-tag-9334ed9625a21b7f
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/src/DraggableMasonryList.tsx b/src/DraggableMasonryList.tsx
index cacb84dbd769c001852547c720696ff2772cbdca..7e45308f31101ce6094165283fcde9995484e7b3 100644
--- a/src/DraggableMasonryList.tsx
+++ b/src/DraggableMasonryList.tsx
@@ -606,7 +606,19 @@ function DraggableMasonryList<T extends MasonryItemType>({
 
     // --- Drag Handlers ---
     const handleDragStart = useCallback((id: string) => {
-        if (!sortEnabled) return;
+        if (!sortEnabled) {
+            console.log("handleDragStart: sortEnabled is false");
+            if (onDragStart) {
+                onDragStart({ key: id, fromIndex: -1 });
+            }
+            return;
+        }
+
+        const current = orderedDataRef.current.find(item => keyExtractor(item) === id);
+        const packageName = current?.["packageName"] ?? "";
+        if (packageName?.includes("@empty")) {
+            return;
+        }
 
         setActiveDragId(id);
         isDraggingShared.value = true;
@@ -616,7 +628,7 @@ function DraggableMasonryList<T extends MasonryItemType>({
             layoutWithoutDraggedRef.current = calculateLayout(
                 withoutDragged, numColumns, containerWidth, rowGap, columnGap, keyExtractor
             );
-        }
+        } 
 
         const currentIndex = orderedDataRef.current.findIndex(item => keyExtractor(item) === id);
         dragStartIndexRef.current = currentIndex;
diff --git a/src/MasonryItem.tsx b/src/MasonryItem.tsx
index e4bbf4c488c5406be4222d7b76fae950bc2ce162..052d499f4c3f6284bef95d4f846d8ba94a94debc 100644
--- a/src/MasonryItem.tsx
+++ b/src/MasonryItem.tsx
@@ -237,7 +237,14 @@ const MasonryItemComponent = ({
 
             runOnJS(onDragEnd)(id);
         });
-
+        
+    const longPressGesture = Gesture.Pan()
+        .enabled(!sortEnabled)
+        .activateAfterLongPress(dragActivationDelay)
+        .onStart(() => {
+            runOnJS(onDragStart)(id);
+        });
+    const composedGesture = Gesture.Simultaneous(panGesture, longPressGesture);
 
     // レイアウトアニメーション用のスタイル（外側のラッパー）
     const wrapperStyle = useAnimatedStyle(() => {
@@ -296,7 +303,7 @@ const MasonryItemComponent = ({
     const exitingAnimation = itemExiting;
 
     return (
-        <GestureDetector gesture={panGesture}>
+        <GestureDetector gesture={composedGesture}>
             {/* 外側: 位置とentering/exitingアニメーション */}
             <Animated.View
                 style={wrapperStyle}
